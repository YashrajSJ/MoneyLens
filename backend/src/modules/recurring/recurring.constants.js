export const DEFAULT_RECURRING_PROCESS_LIMIT = 20;
export const MAX_RECURRING_PROCESS_LIMIT = 50;

export const DEFAULT_RECURRING_LIST_LIMIT = 20;
export const MAX_RECURRING_LIST_LIMIT = 50;

export const MAX_RECURRING_BATCHES_PER_JOB = 10; //One job can process max 20 × 10 = 200 due recurring transactions.
